#!/usr/bin/python3

def divisible_by_2(my_list=[]):

    new_list = []

    for i in my_list:
        if elements % 2 == 0:
            new_list += true
        else:
            new_list += false

    return new_list
